\* TLC model configuration for AssignmentManager.tla
\*
\* Small model: 3 regions, 3 servers.
\* Iteration 8: RS-side open handler and report.  RSReceiveOpen consumes
\* OPEN commands from dispatchedOps, RSCompleteOpen/RSFailOpen produce
\* OPENED/FAILED_OPEN reports in pendingReports.  The ASSIGN round-trip
\* is now complete: dispatch -> RS receive -> RS complete -> report ->
\* master confirm.  FailOpen removed from Next (superseded by RSFailOpen).
\*
\* Preferred: run via MCP tool tlaplus_mcp_tlc_check (handles Java/classpath).
\* Fallback (requires Java 11+, NOT the default temurin-8):
\*   JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home
\*   $JAVA_HOME/bin/java -XX:+UseParallelGC \
\*     -cp "$HOME/.cursor/extensions/tlaplus.vscode-ide-2026.2.250046-universal/tools/tla2tools.jar:$HOME/.cursor/extensions/tlaplus.vscode-ide-2026.2.250046-universal/tools/CommunityModules-deps.jar" \
\*     tlc2.TLC AssignmentManager.tla -config AssignmentManager.cfg -workers auto -cleanup

SPECIFICATION Spec

\* Model values
CONSTANTS
    Regions = {r1, r2, r3}
    Servers = {s1, s2, s3}
    None = None

\* Symmetry reduction: regions and servers are interchangeable
SYMMETRY Symmetry

\* State constraint to bound procedure ID counter
CONSTRAINT
    StateConstraint

\* Invariants to check (all from iterations 1-6)
INVARIANT
    TypeOK
    OpenImpliesLocation
    OfflineImpliesNoLocation
    SingleAssignment
    MetaConsistency
    LockExclusivity
    ProcedureConsistency

\* Action property: every state change follows ValidTransition
ACTION_CONSTRAINT
    TransitionValid
